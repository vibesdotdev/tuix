/**
 * Tests for DebugPerformance.tsx
 */

import { describe, test, expect } from 'bun:test'

describe('DebugPerformance', () => {
  test('should have tests', () => {
    expect(true).toBe(true)
  })
})
