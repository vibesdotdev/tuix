# Testing Plugin

CLI commands for testing TUIX applications: snapshot, benchmark, profile, dashboard.

Uses `@tuix/testing` harness, snapshot matcher, and optional e2e PTY harness
(`createTestHarness` from package root).
