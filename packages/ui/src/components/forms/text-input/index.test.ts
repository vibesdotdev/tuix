/**
 * Tests for index.ts
 */

import { describe, test, expect } from 'bun:test'

describe('UI Components Forms TextInput Index', () => {
  test('should have tests', () => {
    expect(true).toBe(true)
  })
})
