{
  "name": "tuix",
  "private": true,
  "scripts": {
    "build": "bun run ./scripts/build.ts"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@ast-grep/cli": "^0.39.6",
    "@types/bun": "^1.2.23"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
